# 🧪 ScentMatrix Telegram Bot

Backend бот для Mini App ScentMatrix с функциями рассылки и управления пользователями.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd bot
npm install
```

### 2. Настройка

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните значения:

```env
BOT_TOKEN=123456:ABC-DEF1234...      # Токен от @BotFather
ADMIN_ID=123456789                    # Ваш Telegram User ID
MINI_APP_URL=https://your-app.com    # URL вашего Mini App
```

**Как получить:**
- **BOT_TOKEN**: Создайте бота через [@BotFather](https://t.me/BotFather)
- **ADMIN_ID**: Узнайте свой ID через [@userinfobot](https://t.me/userinfobot)
- **MINI_APP_URL**: URL где размещён ваш `index.html`

### 3. Запуск бота

```bash
npm start
```

Для разработки с автоперезагрузкой:

```bash
npm run dev
```

---

## 📋 Команды бота

### Для пользователей:
- `/start` - Запустить приложение
- `/help` - Справка

### Для администратора:
- `/stats` - Статистика пользователей
- `/users` - Список пользователей (до 30)
- `/broadcast <текст>` - Рассылка всем
- `/export` - Экспорт в CSV

---

## 💡 Примеры использования

### Рассылка с HTML:

```
/broadcast <b>🎉 Новые ароматы!</b>

Добавлено 20+ новых парфюмов в базу.

<i>Попробуйте прямо сейчас!</i>
```

### Просмотр статистики:

```
/stats

📊 Статистика ScentMatrix Bot

👥 Всего пользователей: 127
🆕 Новых сегодня: 5
```

---

## 📦 Структура проекта

```
bot/
├── bot.js              # Основной файл бота
├── package.json        # Зависимости
├── .env.example        # Пример конфигурации
├── .env               # Ваша конфигурация (не в Git!)
├── .gitignore         # Исключения для Git
├── users.json         # База пользователей (создаётся автоматически)
└── README.md          # Эта инструкция
```

---

## 🔒 Безопасность

- ✅ Файл `.env` **НЕ должен попадать в Git**
- ✅ Только администратор видит `/users`, `/stats`, `/broadcast`
- ✅ Пользовательские данные хранятся локально в `users.json`

---

## 🌐 Деплой

### Railway.app (рекомендуется)

1. Зарегистрируйтесь на [Railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Добавьте переменные окружения:
   - `BOT_TOKEN`
   - `ADMIN_ID`
   - `MINI_APP_URL`
5. Deploy!

### Render.com

1. Создайте Web Service на [Render](https://render.com)
2. Укажите команду: `npm start`
3. Добавьте Environment Variables
4. Deploy

---

## 🛠️ Разработка

### Добавление новой команды:

```javascript
bot.command('mycommand', (ctx) => {
  if (!isAdmin(ctx.from.id)) {
    return ctx.reply('⛔️ Только для админа');
  }
  
  ctx.reply('Привет, админ!');
});
```

### Обработка callback queries:

```javascript
bot.action('button_id', (ctx) => {
  ctx.answerCbQuery('Нажата кнопка!');
});
```

---

## ❓ FAQ

**Q: Как добавить второго администратора?**

Измените в `bot.js`:

```javascript
const ADMIN_IDS = [123456789, 987654321];

function isAdmin(userId) {
  return ADMIN_IDS.includes(userId);
}
```

**Q: Как настроить Menu Button?**

Через BotFather:
```
/mybots → Выберите бота → Menu Button → Configure Menu Button
```

**Q: Бот не отвечает - что делать?**

1. Проверьте правильность `BOT_TOKEN`
2. Убедитесь что бот запущен (`npm start`)
3. Проверьте логи в консоли

---

## 📝 Лицензия

MIT © VladMarketolog

---

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте `.env` файл
2. Убедитесь что все зависимости установлены
3. Посмотрите логи в консоли
